on:
  push:
    branches: [dev]

name: 🚀 Prod deploy to deploy-fr

jobs:
  web-deploy:
    name: 🎉 Deploy - development
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4
      - name: Use Node.js 14
        uses: actions/setup-node@v4
        with:
          node-version: "16"

      - name: 🔨 Build Project
        run: |
          npm install
          npm run build

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.3.2
        with:
          server: 51.178.176.119
          username: bookho
          password: XU9vIqzyvihU
          server-dir: /portnature/
          exclude: |
            **/database/**
            **/database
            **/public/phpmyadmin
            **/public/phpmyadmin/**
            **/public/assets/images
            **/public/assets/images/**
            **/public/assets/libs
            **/public/assets/libs/**
            **/public/assets/admin/images/**
            **/public/assets/admin/images
            **/public/assets/admin/libs/**
            **/public/assets/admin/libs
            **/vendor
            **/vendor/**
            **/storage
            **/storage/**
            **node_modules/
            **/node_modules/**
            **/.git
            **/.git*/**
