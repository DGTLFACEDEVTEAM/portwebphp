on:
  push:
    branches: [dev]

name: 🚀 Prod deploy to deploy-fr

jobs:
  web-deploy:
    name: 🎉 Deploy - development
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4
      - name: Use Node.js 14
        uses: actions/setup-node@v4
        with:
          node-version: "16"

      - name: 🔨 Build Project
        run: |
          npm install
          npm run build

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.3.2
        with:
          server: 213.159.1.35
          username: portnat
          password: 1U4Sy6VpFi5*j
          server-dir: /home/portnat/portnature/
          exclude: |
            **/database/**
            **/database
            **/public/phpmyadmin
            **/public/phpmyadmin/**
            **/public/assets/frontend/images
            **/public/assets/frontend/images/**
            **/public/assets/frontend/video
            **/public/assets/frontend/video/**
            **/public/assets/frontend/libs
            **/public/assets/frontend/libs/**
            **/public/assets/admin/**
            **/public/assets/admin
            **/public/assets/admin/images/**
            **/public/assets/admin/images
            **/public/assets/admin/libs/**
            **/public/assets/admin/libs
            **/vendor
            **/vendor/**
            **/storage
            **/storage/**
            **node_modules/
            **/node_modules/**
            **/.git
            **/.git*/**
